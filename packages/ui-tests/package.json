{
  "name": "@triangulate/ui-tests",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "cypress": "^9.4.1",
    "eslint-plugin-cypress": "^2.12.1"
  },
  "scripts": {
    "test": "cypress run --headed --no-exit",
    "open": "cypress open --browser chrome",
    "test:ci": "cypress run --headless --browser chrome --record=false"
  }
}
