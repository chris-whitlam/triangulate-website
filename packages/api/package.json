{
  "name": "@triangulate/api",
  "version": "0.1.0",
  "dependencies": {
    "@netlify/functions": "^0.11.0",
    "joi": "^17.6.0",
    "nodemailer": "^6.7.2"
  },
  "devDependencies": {
    "@types/jest": "^24.9.0",
    "@types/nodemailer": "^6.4.4",
    "jest": "^24.9.0",
    "jest-junit": "^13.0.0",
    "ts-jest": "^24.2.0"
  },
  "scripts": {
    "lint": "tsc --noEmit && eslint \"**/*.ts\" --fix",
    "test": "JEST_JUNIT_OUTPUT_DIR=./artifacts jest --coverage"
  }
}
